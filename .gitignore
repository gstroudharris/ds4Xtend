# Python
__pycache__/
*.py[cod]

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Local runtime logs
*.log

# DS4 frontend local runtime state (UI resume across runs) — never committed
lastsession.json
# Conversation logs live in logs/ (kept out of git by logs/.gitignore)
