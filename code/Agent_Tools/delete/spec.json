{
  "function": {
    "name": "delete",
    "description": "Delete a file, or an EMPTY directory (no recursive delete).",
    "parameters": { "type": "object", "properties": { "path": { "type": "string" } }, "required": ["path"] }
  },
  "mutating": true
}
