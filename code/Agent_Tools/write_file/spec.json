{
  "function": {
    "name": "write_file",
    "description": "Create or overwrite a text file with its FULL new contents (not a fragment). For small changes to an existing file, prefer edit_file.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "File path relative to the workspace root." },
        "content": { "type": "string", "description": "The complete file contents (UTF-8, up to ~2 MB)." }
      },
      "required": ["path", "content"]
    }
  },
  "mutating": true
}
